'use strict';

/**
 * showcase-control controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::showcase-control.showcase-control');
